// features/changelog — the player-facing release notes (ClickUp 86eyr3xvf).
//
// The outbound half of the pair with `features/feedback`: report a problem, then
// see it get fixed. Content lives in `shared/data/changelog.ts`, not here — this
// is only how it is rendered.
export { Changelog } from './Changelog.js'
export { WhatsNew } from './WhatsNew.js'
